const styles = {
  container: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100vh',
  },
}

function Homepage() {
  return (
    <div style={styles.container}>
      <h1>Home Page</h1>
    </div>
  )
}

export default Homepage
